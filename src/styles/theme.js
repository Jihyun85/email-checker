const fontSizes = {
  xs: "12px",
  sm: "14px",
  md: "16px",
  lg: "18px",
  xl: "20px",
  smTitle: "28px",
  mdTitle: "32px",
  lgTitle: "40px",
};

const colors = {
  mainColor: "#74B9FF",
  hoverBlue: "#5c92ff",
  lightGray: "#F5F5F5",
  hoverGray: "#ebebeb",
  darkGray: "#808080",
  green: "#1E6F43",
};

const event = {
  focusBorder: "0 0 2px 2px rgba(52, 73, 94, 0.8)",
};

const theme = {
  fontSizes,
  colors,
  event,
};

export default theme;
